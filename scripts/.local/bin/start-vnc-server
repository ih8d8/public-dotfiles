#!/bin/bash

SSHD_PID=$(ps -fC sshd --no-headers | awk '{print $2}')
if [ -z "${SSHD_PID}" ]; then
  sudo systemctl start sshd.service
fi

if [ "${XDG_SESSION_TYPE}" == "wayland" ]; then
    wayvnc
else
    x0vncserver -interface 127.0.0.1 -rfbauth ~/.vnc/passwd
fi