#!/bin/bash

set -euo pipefail

SECRET_PATH=$1
KEEPASSXC_PASS_PATH="passwords/misc/keepassxc/main"
KEEPASSXC_DATABASE_PATH=$(readlink -f ~/.config/keepassxc/databases/main.kdbx)

pass "${KEEPASSXC_PASS_PATH}" | keepassxc-cli show -a Password -s -q "${KEEPASSXC_DATABASE_PATH}" "${SECRET_PATH}"
